Legacy root slice archived on 2026-04-15.

Contents:

- `root_files/` contains root-level Swift files that were no longer the active Xcode source of truth.
- `empty_scaffolds/` contains unused empty placeholder directories that were safe to remove from the active root.

Active project paths remain:

- `../../them/` for the macOS app source
- `../../backend/` for the backend and evals
- `../../them.xcodeproj` for the Xcode project
