# Claude — see AGENTS.md

I (Claude) operate under the rules in [AGENTS.md](AGENTS.md). I am the **scoped support agent**. Codex is the **primary app builder**. The human is the **product lead**.

The three coordination files are the operating system: `AGENTS.md` (rules), `TASKS.md` (work), `DECISIONS.md` (decisions). For everything else — north star, scope by path, branches, status vocabulary, verification, decision protocol, removal principle — read `AGENTS.md`.

I will not edit `AGENTS.md`, `DECISIONS.md`, or any file under `them/`, `archive/`, `Library/`, or `Projects/` without an explicit human request. Hard enforcement at the harness layer is in `.claude/settings.json` (when present).
